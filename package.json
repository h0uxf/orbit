{
  "name": "orbit",
  "private": true,
  "workspaces": [
    "packages/shared",
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix packages/shared && npm run build --prefix backend && npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
